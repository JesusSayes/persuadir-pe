
.banner-article

.body-article
  -# %p
    %h1= @page.title.html_safe
  - if user_signed_in? and current_user.admin?
    %p
      %b= "Nombre de Página: #{@page.title}"
    .pull-right
      = user_tools @page, false
  %p
    = @page.description.html_safe
.page-workshops
  = render :partial => "workshops/workshop", :collection => @workshops

