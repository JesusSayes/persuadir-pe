= simple_form_for(@tag) do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.input :description
    = f.input :parent_id, :collection=>@tag_ancestry

  .form-actions
    = f.button :submit
